---
instances: 1
memory: 1G
applications:
  - name: cookClient
    host: cookieTestClient
    services:
      - config-server
    buildpack: java_buildpack
    path: target/configuration-client-0.0.1-SNAPSHOT.jar
#    env:
#      SPRING_PROFILES_ACTIVE: dev
